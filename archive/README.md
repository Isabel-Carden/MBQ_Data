# Archive

Older drafts, duplicates, and tutorials live here so nothing is lost while the
paper scripts stay easy to find in `../scripts/`.

See `../logs/CHANGELOG.md` for the dated list of what was moved and why.
